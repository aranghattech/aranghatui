# Changesets

All `@aranghat/*` packages are released in lockstep (one `fixed` group). Add a changeset with `pnpm changeset` in any PR that changes a published package. Nothing is published before 1.0 (ADR-0016); `release.yml` is manual and gated.
