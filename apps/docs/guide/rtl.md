# RTL

Set `dir="rtl"` on `<html>` (or any ancestor). Components use logical properties (`padding-inline`, `inset-inline-start`) so they mirror without extra CSS. Directional components are screenshot-tested in both directions.
