// Reference budget: full admin shell (all tiers, via the @aranghat/ui meta-package) ≤ 180 kB gzip — CLAUDE.md §7.
import '@aranghat/tokens/aranghat.css';
import { defineAll } from '@aranghat/ui';

defineAll();
